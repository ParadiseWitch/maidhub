class FileUtil {
  
  constructor(parameters) {
    
  }

  /**
   * //TODO 判断文件是否存在
   * @returns 
   */
  public static isFileExisted(): boolean{
    return 
  }

  // TODO 读写文件（同步/异步）、创建（临时）文件、删除文件、重命名
}